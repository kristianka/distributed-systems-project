# Backend Server Configuration
#
# Both environment variables are REQUIRED.

# This node's ID (must match one entry in CLUSTER_NODES)
NODE_ID=node-a

# All nodes in the cluster (REQUIRED)
# Format: nodeId:host:port:rpcPort,nodeId:host:port:rpcPort,...
#   - nodeId: Unique identifier for this node
#   - host: Hostname/IP where this node is reachable by other nodes
#   - port: WebSocket port for frontend client connections
#   - rpcPort: Port for inter-node RPC communication

# Local development (all nodes on localhost - need different ports):
CLUSTER_NODES=node-a:localhost:8741:9741,node-b:localhost:8742:9742,node-c:localhost:8743:9743

# Production/different servers (can use same port since they're on different machines):
# CLUSTER_NODES=node-a:server1.example.com:8741:9741,node-b:server2.example.com:8741:9741,node-c:server3.example.com:8741:9741

# 5 servers (more fault tolerance):
# CLUSTER_NODES=node-a:b1.example.com:8741:9741,node-b:b2.example.com:8741:9741,node-c:b3.example.com:8741:9741,node-d:b4.example.com:8741:9741,node-e:b5.example.com:8741:9741

# Note: For Raft consensus, use an odd number of nodes (3, 5, 7...)
# - 3 nodes: tolerates 1 failure
# - 5 nodes: tolerates 2 failures
# - 7 nodes: tolerates 3 failures
